Answer1:-
git clone copies the remote repo into the local system while the git init  creates a new repo on the directory you opened that time

Answer2:-
git status tells us about the status of file ,wheather its tracked or not or the file is in staged area or not

Answer3:-
git add adds the file to the staged area from local directory

Answer4:-
git diff tells us about the changes done in untracked files while the git diff --staged tells us about the differnce or changes done in the file which is in stagged area or of tracked file

Answer5:-
git commit is point where we saved the project's git history and keep records a set of changes with some information such as : author,repo state , message etc

Answer6:-
git log gives the complete info about the commits history with author name , email, date , time etc with commit id of each previous commit where git log --oneline give the main messge of commit and an unique id only ,for each commit there is only on line 

Answer7:-
git show shows information about the last commit or git object or git project , generally it shows the commit with commit id , author, date , commit message , change introduced by the commit

Answer8:-
git pull gives us changes and repo in our local system

Answer9:-
git push made changes in the remote repo which you done in the local directory

Answer10:-
git branch is used to figure out how many repo's are there and in which branch we are currently in 

Answer11:-
git checkout is used to change branch in terminal or local system

Answer12:-
  because if we directly made changes into main branch and if there will be any mistake then my whole file will be corrupt . so we want branches where we can code first and pull request and then others can see it and rivew it And if there will be any mistake than they will correct it and merge it

Answer13:-
a pull request is request to merge the branch into main branch after review it

Answer14:-
because if there will be any mistake in the code in the branch which is requested to review and if don't review it merge it into the main branch then the whole file can be corrupted

Answer15:-
main is the our main repo  of our github and  feature pages is the branch made from the main repo as a source
